/* Block Quote Data */
export type DriveDirectBlockQuoteData = {
    quote: string,
    caption: string
}

export const driveDirectBlockQuoteData: DriveDirectBlockQuoteData = {
    quote: "Google Drive direct links are meant for simple use cases, not as a CDN. They are not recommended for production or high-traffic applications.",
    caption: "— Drive Direct"
}